/// Gender types for male and female
enum Gender { male, female }

/// CallTypes for calls
/// audio : complete call was audio call
/// bideo: complete call was Video call
/// mixed: call was switched and involved both audio and video calls
enum CallType { audio, video, mixed }

/// [CallDirection] referes to Outgoing call or Incoming call
enum CallDirection { outgoing, incoming }
